/**
 * Domain model: JPA entities and domain enums. Every work-time record carries
 * its origin ({@code confirmado} / {@code auto} / {@code reconstruido}) —
 * evidential honesty is a golden rule of the project (see CLAUDE.md).
 */
package es.medeben.domain;
